# This project reuses the existing tfstate bucket: gcpai-iac-tfstate
# and existing GCP projects: gcpai-iac-dev, gcpai-iac-prod
# No new projects need to be created for this portfolio project.

# Hub VPC will be created inside gcpai-iac-dev project
# Dev Spoke VPC will be created inside gcpai-iac-dev project
# Prod Spoke VPC will be created inside gcpai-iac-prod project
